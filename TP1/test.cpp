#include <iostream>
#include <string>
#include <ctype.h>

using namespace std;

int main(int argc, char ** argv)
{
  /* string texte (argv[1].toString()), crypte;
    int decalage(0);
 
    // Demande du decalage a l'utilisateur
    cout << "Quel decalage voulez-vous utiliser ? ";
    cin >> decalage;


    
    for (string::iterator it=texte.begin(); it!=texte.end(); ++it)
    {
        if (isupper(*it))
        {
            crypte += (*it + decalage - 'A')%26 + 'A';
            crypte += ", ";
        }
        else
        {
	  crypte += (*it + decalage - 'a')%26 + 'a';
            crypte += ", ";
        }
    }
 
    cout << crypte;

    */
  return 0;
}

